#!/usr/bin/env node --experimental-modules

import('../src/til.mjs')
  .then(({ main }) => main(process.argv))
  .catch(error => {
    console.error(`Failed. ${error || ''}`)
    process.exit(1)
  })
